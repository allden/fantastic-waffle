<!DOCTYPE html>
<html lang="en-US">
    <head>
        <%- include('partials/head'); %>
        <title>Sanity Check</title>
    </head>
    <body>
        <%- include('partials/nav'); %>
        <div class="container-fluid d-flex flex-row align-items-center justify-content-center viewport-fit">
            <div class="jumbotron bg-complement text-center w-75">
                <%- include('partials/messages'); %>
                <h1><%= user ? `Welcome, ${user.username}` : 'Welcome!'%></h1>
                <p>This website is for websocket testing purposes.</p>
            </div>
        </div>
        <%- include('partials/scripts'); %>
    </body>
</html>